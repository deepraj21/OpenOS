# OpenOS Composer (`@openos/composer`)

Next.js + [`@xyflow/react`](https://reactflow.dev/) canvas for wiring agents visually. Layout chrome follows [`DESIGN.md`](../../DESIGN.md) spacing and typography fallbacks.

```bash
pnpm --filter @openos/composer dev
```

Export-to-`createOS()` code generation is intentionally not wired yet; the canvas is the integration surface for that follow-up.
